//! VT state engine. Will eventually wrap libghostty-vt so the backend can
//! evaluate escape sequences server-side and answer reconnect queries with a
//! coherent screen state.
//!
//! Empty for now — the xterm.js renderer drives behavior until this lands.
