{
  "name": "relayterm",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "check": "pnpm -r check",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "check:docs-contracts": "bash scripts/check-doc-contracts.sh"
  },
  "packageManager": "pnpm@10.33.0"
}
